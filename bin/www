#!/usr/bin/env node
var debug = require('debug')('Apidoc');
var app = require('../app');

app.set('port', process.env.OPENSHIFT_NODEJS_PORT || 80);

var server = app.listen(app.get('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});
